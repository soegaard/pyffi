#lang info

(define compile-omit-paths 'all)

(define deps '("base"))
(define build-deps '())

(define pkg-desc "Use Python from Racket - Test Suite")

(define pkg-authors '(soegaard))

(define version "1.0")

(define test-responsibles '((all jensaxel@soegaard.net)))
